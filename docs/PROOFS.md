# The elliptical collapse, proved

Regenerated by `python3 -m tools.ringing_measure.proofs`.

```

==========================================================================
PROOF 1. The ellipse is the constant: trace(G) = N for any ensemble
    cited by: THE_ALGORITHM.md section 3; ELLIPTICAL_COLLAPSE.md section 2
==========================================================================
             independent: trace 4.0000000000
               collinear: trace 4.0000000000
  wildly different norms: trace 4.0000000000
  [PASS] trace equals the component count whatever the components do
         got worst departure 1.78e-15, expected < 1e-9
  -- It is an identity of unit-normalising each row, not a discovery. What it licenses is the next sentence: only the SHAPE carries information.

==========================================================================
PROOF 2. The sphere floor N/(L+N-1), derived and measured
    cited by: THE_ALGORITHM.md section 5; ELLIPTICAL_COLLAPSE.md section 3
==========================================================================
  N=  3 L=  256: predicted 0.01163  measured 0.01187  ratio 1.021
  N=  4 L=  256: predicted 0.01544  measured 0.01531  ratio 0.991
  N=  8 L=  256: predicted 0.03042  measured 0.03041  ratio 1.000
  N= 16 L=  256: predicted 0.05904  measured 0.05973  ratio 1.012
  N= 32 L=  256: predicted 0.11150  measured 0.11215  ratio 1.006
  [PASS] measured over predicted has no trend and stays near one
         got 0.971 to 1.026, expected 0.85 to 1.20
  -- The formula is a delta-method approximation that happens to be accurate. Its SCATTER is a separate matter - see proof 14, which shows the shipped scatter is the complex large-N asymptote and wrong for a real ensemble.

==========================================================================
PROOF 3. The Marchenko-Pastur edge is where noise stops
    cited by: THE_ALGORITHM.md section 4
==========================================================================
  N=  4 L=   64: edge 1.5625  largest eigenvalue 1.2848  ratio 0.822
  N=  4 L=  256: edge 1.2656  largest eigenvalue 1.1412  ratio 0.902
  N=  4 L= 1024: edge 1.1289  largest eigenvalue 1.0759  ratio 0.953
  N=  8 L=   64: edge 1.8321  largest eigenvalue 1.5633  ratio 0.853
  N=  8 L=  256: edge 1.3848  largest eigenvalue 1.2647  ratio 0.913
  N=  8 L= 1024: edge 1.1846  largest eigenvalue 1.1247  ratio 0.949
  N= 16 L=   64: edge 2.2500  largest eigenvalue 1.9648  ratio 0.873
  N= 16 L=  256: edge 1.5625  largest eigenvalue 1.4488  ratio 0.927
  N= 16 L= 1024: edge 1.2656  largest eigenvalue 1.2150  ratio 0.960
  [PASS] on noise the largest eigenvalue stays below the edge at these sizes
         got worst 0.960, expected < 1.000
  -- The shortfall is the finite-size Tracy-Widom effect and shrinks with L. At larger sizes the exceedance rate settles at a few per cent, so 'never admits noise' is true only at the sizes tested. The edge does NOT apply at all to a nested matrix - see proof 12.

==========================================================================
PROOF 4. The Wiener weight equals the correction-gain law
    cited by: THE_ALGORITHM.md section 6; ELLIPTICAL_COLLAPSE.md section 3.1
==========================================================================
  lambda  1.20  SNR  0.20  (l-1)/l 0.166667  1/(1+rho) 0.166667
  lambda  1.50  SNR  0.50  (l-1)/l 0.333333  1/(1+rho) 0.333333
  lambda  2.00  SNR  1.00  (l-1)/l 0.500000  1/(1+rho) 0.500000
  lambda  3.00  SNR  2.00  (l-1)/l 0.666667  1/(1+rho) 0.666667
  lambda  6.00  SNR  5.00  (l-1)/l 0.833333  1/(1+rho) 0.833333
  lambda 12.00  SNR 11.00  (l-1)/l 0.916667  1/(1+rho) 0.916667
  [PASS] the two expressions are identical for every eigenvalue
         got worst difference 1.11e-16, expected < 1e-12
  [PASS] the flat half-step is the case SNR = 1
         got 0.5, expected 0.5
  -- This is a rearrangement of one MMSE expression, not two derivations meeting. And the two rho are different quantities: this one is read from the same ensemble the ellipse was fitted to, which the correction-gain law's own record says returns 0.00 where the truth is 2.47.

==========================================================================
PROOF 5. Residuals on different grids are never projected
    cited by: COMPONENT_MAPPINGS.md section 9
==========================================================================
  a 238-bin response against a 262-line residual: coherence 0.0000
  both left untouched by the projection: True
  [PASS] different abscissae give no coherence at all
         got 0.0, expected 0.0
  [PASS] and neither is modified
         got True, expected True
  -- Before the repair the two were truncated to a common length and dotted together, which returns a number that is an artefact of memory layout.

==========================================================================
PROOF 6. The projection keeps the phase
    cited by: COMPONENT_MAPPINGS.md section 9; ELLIPTICAL_COLLAPSE.md section 8
==========================================================================
  output dtype complex128, imaginary energy 8.187
  asymmetry with the phase 0.1918, with it stripped 0.8740
  [PASS] the projection returns a complex value
         got complex128, expected complex
  [PASS] stripping the phase answers a different question
         got 0.682, expected > 0.2
  -- np.asarray(complex, dtype=float64) keeps the real part and drops the imaginary one. It is not a precision loss.

==========================================================================
PROOF 7. A gain and a delay of the same shape are two mechanisms
    cited by: THE_ALGORITHM.md section 3; COMPONENT_MAPPINGS.md section 9
==========================================================================
  Hermitian inner product : rank 1, asymmetry 1.0000
  real-stacked            : rank 2, asymmetry 0.0000
  [PASS] under the Hermitian product they are one direction
         got 1, expected 1
  [PASS] stacking real and imaginary parts makes them two
         got 2, expected 2
  -- Correct when the complex value IS the measurement; wrong when each component is a real parameter's signature.

==========================================================================
PROOF 8. Tape losses collapse: one dimensionless group, five lengths
    cited by: COMPONENT_MAPPINGS.md section 2; ELLIPTICAL_COLLAPSE.md section 7.4
==========================================================================
  singular values [1.8436 1.107  0.5994 0.1277 0.    ]
  effective distinct shapes 1.90 of 5
  [PASS] five mechanisms span far fewer than five shapes
         got 1.90, expected < 2.5
  -- Every one is a function of ONE dimensionless group, a length over the recorded wavelength. As far as a frequency response is concerned they are five values of the same variable.

==========================================================================
PROOF 9. Echoes do not collapse: a Fourier basis over delay
    cited by: COMPONENT_MAPPINGS.md section 2
==========================================================================
  eight echoes spaced at 1/B: effective 7.92 of 8, condition 1.18
  asymmetry 0.0112 - very nearly a sphere
  [PASS] an echo family is nearly fully independent
         got 7.92, expected > 7.5
  [PASS] and superbly conditioned
         got 1.18, expected < 2.0
  -- exp(-2 pi j f tau) is a Fourier basis over delay, orthogonal by construction. A family is collinear when its members differ in a RATE and separable when they differ in KIND.

==========================================================================
PROOF 10. Only particle noise is random; four disturbances are not
    cited by: COMPONENT_MAPPINGS.md section 7; ELLIPTICAL_COLLAPSE.md section 7.7
==========================================================================
        particle noise: 0 direction(s), asymmetry 0.0307 vs floor 0.0304, +0.1 sigma
     beat / co-channel: 1 direction(s), asymmetry 1.0000 vs floor 0.0304, +351.0 sigma
     head contact tilt: 1 direction(s), asymmetry 1.0000 vs floor 0.0304, +351.0 sigma
  [PASS] particle noise sits at the sphere floor
         got +0.1 sigma, expected within 3.0
  [PASS] a beat has a frequency even when its phase is random
         got +351 sigma, expected > 50
  -- Every disturbance here is given a RANDOM PHASE per field, so none can correlate trivially.

==========================================================================
PROOF 11. The specification's recording depth is lambda/4, not a constant
    cited by: ELLIPTICAL_COLLAPSE.md section 7.6; interference.particle_noise
==========================================================================
  the guide works d = lambda/4 to 0.253-0.319 um; it prints 0.26-0.36 um
  slope with the law   +20.0 dB/decade
  slope at fixed depth +10.0 dB/decade
  [PASS] the read volume goes as lambda squared
         got +20.0 dB/decade, expected +20.0
  [PASS] the transcription reproduces the guide's own worked range
         got 0.253-0.319 um, expected 0.26-0.36 um
  -- JVC VTG82063 section 7.2: 'the optimum playback sensitivity is obtained when recording is performed at a depth equivalent to 1/4th the recording wavelength'.

==========================================================================
PROOF 12. WITHDRAWN: pure noise reproduces the nested-matrix run
    cited by: ELLIPTICAL_COLLAPSE.md section 6c
==========================================================================
  frequency / head A
      reported : rank 13, 11 directions, 92.9% resolved, +39.3 sigma
      PURE NOISE: rank 13, 12 directions, 99.1% resolved, +37.9 sigma
  time / head A
      reported : rank 13, 13 directions, 100.0% resolved, +118.0 sigma
      PURE NOISE: rank 13, 12 directions, 98.9% resolved, +116.3 sigma
  [PASS] the defect still reproduces: noise gives the same answer as the reported run
         got within 6 sigma and 10 points, expected the defect
  -- component_differentials emits N(N+1)/2 entries spanning only N dimensions, so the rank is forced, every eigenvalue clears the noise edge, and the shape statistics measure the construction. This proof FAILS if the defect stops reproducing, which would mean the withdrawal in section 6c no longer matches the code.

==========================================================================
PROOF 13. The exact 0.000 asymmetry is a literal constant
    cited by: ELLIPTICAL_COLLAPSE.md section 6c
==========================================================================
  removed=0 : asymmetry 0.088888
  removed=5 : asymmetry 0.000000  (effective clips to 1)
  [PASS] once effective reaches one the expression returns the else branch
         got 0.0, expected 0.0
  -- So 'the asymmetry falls to exactly zero' is that constant, not a measurement, whenever significant equals rank.

==========================================================================
PROOF 14. The shipped scatter is the complex asymptote, wrong for real data
    cited by: ELLIPTICAL_COLLAPSE.md section 6c
==========================================================================
  N=  8 L=1024,     real: scatter x L = 2.152   (the code uses 1.414)
  N=  8 L=1024,  complex: scatter x L = 1.331   (the code uses 1.414)
  N= 32 L=1024,     real: scatter x L = 1.914   (the code uses 1.414)
  N= 32 L=1024,  complex: scatter x L = 1.229   (the code uses 1.414)
  [PASS] a real ensemble scatters about 2/L, not sqrt(2)/L
         got 1.91 to 2.15, expected > 1.7
  [PASS] sqrt(2)/L is the COMPLEX asymptote
         got 1.280, expected 1.414
  -- Every sigma reported on a real ensemble is inflated by about 1.4x, and when N approaches L the true scatter collapses further still.

==========================================================================
PROOF 15. The surrogate null is the only valid one for a constructed ensemble
    cited by: ELLIPTICAL_COLLAPSE.md section 6c; information_extrapolation.surrogate_null
==========================================================================
          pure noise: observed 0.8862, null 0.8858 +- 0.0016, z +0.2
    a departure x1.5: observed 0.8817, null 0.8858 +- 0.0016, z -2.5
      a departure x4: observed 0.8862, null 0.8858 +- 0.0016, z +0.2
  the null rebuilds the construction: 13 independent draws, deficiency 78
  and where the departure's energy goes:
      x0.0 : diagonal    124.3, off-diagonal    249.6   (13 entries vs 78)
      x1.5 : diagonal    418.6, off-diagonal    249.6   (13 entries vs 78)
      x4.0 : diagonal   2184.6, off-diagonal    249.6   (13 entries vs 78)
  [PASS] noise does not stand apart from a null built the same way
         got +0.2, expected within 3
  [PASS] AND NEITHER DOES A SHARED DEPARTURE, however strong - the asymmetry on a nested matrix is BLIND to it
         got noise +0.2, x1.5 -2.5, x4 +0.2, expected all within 3, i.e. blind
  -- THE DEFECT, IN ITS SHARPEST FORM. A shared departure cancels exactly in every difference - off-diagonal energy is unchanged at 249.6 whatever the departure does - so it enters only through the 13 diagonals against 78 off-diagonals. And because every row is unit-normalised, growing the diagonals seventeenfold does not move their DIRECTIONS, so the shape is unchanged: at x4 the asymmetry reads 0.8862 against pure noise's 0.8862. The statistic could not see the thing it was supposed to measure, which is the whole of why the run in section 6 reported the construction. The remedy is not a better null on this matrix - it is to fit the ellipse over TRANSFERS rather than differences, which is what section 0a now does.

==========================================================================
PROOF 16. Representable is not knowable: a full basis spans all noise
    cited by: THE_ALGORITHM.md, 'Representable is not knowable'
==========================================================================
  a basis of   8 sinusoids spans   1.2% of pure noise   (equal share   3.1%)
  a basis of  64 sinusoids spans  21.1% of pure noise   (equal share  25.0%)
  a basis of 200 sinusoids spans  77.1% of pure noise   (equal share  78.1%)
  a basis of 256 sinusoids spans 100.0% of pure noise   (equal share 100.0%)
  [PASS] a basis captures exactly its equal share of noise and no more
         got worst departure 0.039, expected < 0.05
  [PASS] a complete basis represents noise perfectly
         got 100.0%, expected 100%
  -- Spanning is not predicting. This is exactly how a construction whose entries spanned the whole space came to report noise as structure.

==========================================================================
PROOF 17. The key must span, not merely be large
    cited by: COMPONENT_MAPPINGS.md section 8a
==========================================================================
  without the specified emphasis : 8.56 of 13, condition 6.8
  with it, as its level dependence: 8.45 of 14, condition 7.4
  with a near-duplicate added    : 7.95 of 15, condition 114494619344754.3
  [PASS] a second copy of a direction already in the span LOWERS the effective count
         got 7.95 against 8.45, expected lower
  -- A modification earns a place in the key by the DIRECTION it adds, not by being a modification. When a standard hands over a family of curves, enter what VARIES between them.

==========================================================================
PROOF 18. The three-way split: inside, structured-but-outside, and noise
    cited by: interference.split_residual
==========================================================================
              pure noise: inside   3.4%  structured   0.0%  noise  96.6%
         a modelled tilt: inside  60.6%  structured   0.0%  noise  39.4%
     an UNMODELLED shape: inside   1.8%  structured  68.5%  noise  29.7%
  [PASS] pure noise lands almost entirely in the third part
         got 96.6%, expected > 90%
  [PASS] a modelled mechanism lands inside the key
         got 60.6%, expected > 40%
  [PASS] an unmodelled mechanism is found OUTSIDE the key and still structured
         got 68.5%, expected > 40%
  -- The middle part is the only thing that is both unreached and reachable, and it is separable from noise only because the surrogate null exists.

==========================================================================
PROOF 19. The capture profile is read, not assumed
    cited by: capture_profile.measure_bit_depth
==========================================================================
  word length 8 bit, step 256 in the container
  117 codes present, occupying 45.7% of full scale
  quantisation SNR 49.9 dB
  [PASS] an 8-bit capture in a 16-bit container reads as 8-bit
         got 8.0, expected 8.0
  [PASS] and occupancy is not mistaken for full scale
         got 0.457, expected < 0.6
  -- The full scale is the container's range. Deriving it from the data's span reports a quiet capture as having fewer bits than it has.

==========================================================================
PROOF 20. The radio binds, not the converter - and at what C/N that reverses
    cited by: ELLIPTICAL_COLLAPSE.md section 7.1; COMPONENT_MAPPINGS.md section 8
==========================================================================
  measured spread 0.697 dB: tape 21.6 dB -> 57.4 Mbit/s; capture 46.7 dB -> 124.0
  the TAPE binds, by 2.16x
  a pristine tape (0.005 dB) instead binds on the CAPTURE
  the crossover sits near 46.6 dB tape C/N
  [PASS] on this capture the tape is the limit
         got tape, expected tape
  [PASS] the verdict is a measurement, not a foregone conclusion
         got capture, expected capture
  -- The envelope-to-C/N conversion is 3.01 dB generous on the tape side only, and Carson's deviation should be the 0.5 MHz peak rather than the 1.0 MHz swing. Both move the numbers; neither moves the ordering.

==========================================================================
PROOF 21. A dimension is the Wiener transfer between two channels
    cited by: THE_ALGORITHM.md section 0a; pair_dimension
==========================================================================
  a known one-pole channel: gain error 2.75%, phase error 1.61 degrees
    4 segments on INDEPENDENT channels: raw coherence 0.2476 (= 1/n = 0.2500), debiased 0.1036
   16 segments on INDEPENDENT channels: raw coherence 0.0575 (= 1/n = 0.0625), debiased 0.0195
   64 segments on INDEPENDENT channels: raw coherence 0.0153 (= 1/n = 0.0156), debiased 0.0052
  [PASS] the transfer is recovered in gain and phase
         got 2.8% and 1.6 deg, expected < 10% and < 5 deg
  [PASS] an uncorrelated pair returns exactly 1/n before debiasing
         got within 30% of 1/n, expected 1/n
  -- |T|, arg T and the frequency it lives at are the amplitude, phase and frequency of the differential pair.

==========================================================================
PROOF 22. The information a VHS field holds
    cited by: ELLIPTICAL_COLLAPSE.md section 7.6
==========================================================================
  the plane's extent: 417,084 independent complex points per field
    raw envelope spread, corrected:  18.6 dB ->   722.0 kbit/field, 1.73 bits per point
        after separating structure:  33.1 dB ->  1284.2 kbit/field, 3.08 bits per point
          the particle-noise floor:  41.1 dB ->  1594.5 kbit/field, 3.82 bits per point
  [PASS] Carson's band uses the PEAK deviation
         got 7.0 MHz, expected 7.0 MHz
  [PASS] separating structure raises what the tape is worth
         got monotone, expected monotone
  -- A VHS field holds between 722 and 1284 kbit depending on how much of the envelope variation is structure - the one quantity the transform exists to measure.

==========================================================================
PROOF 23. Every key entry is subtractable, and the key has a known order
    cited by: MATHEMATICS.md section 3; interference.subtractable, ordered_key
==========================================================================
  14 entries, 14 subtractable
  order runs from position 30 down to 1 - last applied, first removed
  an undeclared entry is refused: True
  [PASS] every key entry has a finite logarithm, so it can be subtracted
         got 0 failing, expected 0
  [PASS] a key entry with no declared position is refused, because a chain can only be inverted in the reverse of its order
         got True, expected True
  -- Two ways an entry used to fail: a MASK, zero outside its support, and a POWER contribution, zero where the interferer is not. Both now enter in bounded form - 1 - d*shape and 1 + a*shape - and enforcing that raised the effective count from 6.92 to 7.66 and improved the condition from 11.4 to 7.3.

==========================================================================
PROOF 24. What no model reaches can be reconstructed if it is band-limited
    cited by: interference.interpolate_unmodelled
==========================================================================
   a SMOOTH unmodelled shape: concentration  16.2% against an equal share of 4.2%, gain 3.85
    a ROUGH unmodelled shape: concentration   0.3% against an equal share of 4.2%, gain 0.08
                  pure noise: concentration   1.3% against an equal share of 4.2%, gain 0.32
  [PASS] a smooth departure the key cannot model is recoverable by band-limited interpolation
         got 3.85x, expected > 3x
  [PASS] noise and a rough shape are not, and report their own equal share
         got 0.32x and 0.08x, expected < 1.5x
  -- The band must be narrow or the test cannot discriminate: at a quarter of the transform the equal share is already fifty per cent and a rough shape scores higher than a smooth one.

==========================================================================
PROOF 25. A remainder is the floor only if it has the floor's SHAPE
    cited by: interference.noise_shape_differential
==========================================================================
   particle noise, correct shape: slope   19.7 dB/decade against an expected 20.0, departure -0.3 -> floor: True
               white noise, flat: slope    0.3 dB/decade against an expected 20.0, departure -19.7 -> floor: False
                 a falling shape: slope   -9.5 dB/decade against an expected 20.0, departure -29.5 -> floor: False
  [PASS] particle noise is recognised as the floor
         got yes, expected yes
  [PASS] a flat remainder is NOT the floor, however small
         got no, expected no
  -- The read volume goes as the wavelength squared because the standard fixes the recording depth at a quarter wavelength, so particle noise rises at twenty decibels per decade and nothing else in the modelled set does. That gives the noise a synthetic side of its own.

==========================================================================
PROOF 26. Have we used all the information? The composite verdict
    cited by: interference.information_used
==========================================================================
            only what the key models: exhausted True, inside 62.1%, structured 0.0%
                                      -> nothing the present key can reach
    plus something it does NOT model: exhausted False, inside 30.9%, structured 48.2%
                                      -> 48.2% of the residual is structured and outside the key
  [PASS] with only modelled mechanisms present the key has reached everything it can
         got True, expected True
  [PASS] and it says so when something is outside it
         got False, expected False
  -- Four conditions, all required: the key is ordered, every entry is subtractable, nothing structured remains outside it, and what does remain sits at the null. The first two are preconditions - an unordered or unsubtractable key can appear to leave nothing behind simply by being unable to remove anything properly.

==========================================================================
PROOF 27. The final dimension: the sync as specified against the sync as it came back
    cited by: pair_dimension.spec_sync, sync_dimension
==========================================================================
  the specified pulse: width 4.700 us (4.700), 10-90 fall 140 ns (140), depth -40.0 IRE (-40)
  a known channel recovered from it: gain error 2.9%, phase error 2.3 degrees, coherent over 20 bins
  [PASS] the pulse is the specified width
         got 4.700 us, expected 4.700 us
  [PASS] and the specified 10-90 rise
         got 140 ns, expected 140 ns
  [PASS] the channel applied to it is recovered
         got 2.9% and 2.3 deg, expected < 15% and < 8 deg
  -- The best-conditioned dimension available, for three reasons no other pair has together: its synthetic side is SPECIFIED rather than fitted, it is CONTENT-FREE so the sync-only constraint is satisfied, and the pulse exists on BOTH sides of the demodulator so a residual on one is a prediction about the other. The standard states a 10-90 time, and a raised cosine's 10-90 is 0.5904 of its transition - widening by that reciprocal is what takes the pulse from 100 ns to 140.

==========================================================================
PROOF 28. A constant is unknowable, and the standard is what supplies it
    cited by: standard_levels; MATHEMATICS.md section 9
==========================================================================
  a constant gain or offset moves no direction: 1, 1, 1 directions
  source gain 1.0, offset +0: anchors  -40.00 /   0.00 -> white lands at 100.0 IRE
  source gain 1.7, offset +12: anchors  -56.00 /  12.00 -> white lands at 100.0 IRE
  source gain 0.6, offset -5: anchors  -29.00 /  -5.00 -> white lands at 100.0 IRE
  [PASS] source equipment levels are in the null space of any differential
         got no direction moved, expected no direction moved
  [PASS] and the standard's own levels supply what the measurement cannot see
         got [100. 100. 100.], expected 100.0 IRE
  -- A gain rescales every component alike and an offset shifts them alike, so neither moves a DIRECTION. That layer is the source equipment's, it is constant over the signal, and no differential can reach it - which is exactly what a specification is for.

==========================================================================
PROOF 29. The closing step: the residual over the tape's ideal response
    cited by: interference.ideal_tape_response, interpolate_over_ideal
==========================================================================
  self-demagnetisation caps the depth at lambda/(2 pi), not the lambda/4 optimum: 1.96 dB, everywhere
          only what the key models: in band  84.4%, recoverable   0.8%, ABSOLUTE FLOOR  83.7%
    plus a smooth unmodelled shape: in band  84.2%, recoverable  14.2%, ABSOLUTE FLOOR  70.0%
                        pure noise: in band  84.3%, recoverable   0.9%, ABSOLUTE FLOOR  83.4%
  [PASS] the optimum depth is never reachable, and the cost is exactly 10 log10((1/4)/(1/2pi))
         got 1.96 dB, expected 1.96 dB
  [PASS] and the noise slope is unchanged, because both laws scale with the wavelength
         got -20.0 dB/decade, expected -20.0
  [PASS] recovering an unmodelled but band-limited shape lowers the floor
         got 70.0%, expected lower
  -- The capture holds five to nine times what the tape has to say, so the capture's band is WIDER than the tape's and anything the tape's response could never have carried is not tape information. Weighting by the ideal response is therefore not a smoothing choice - it is discarding what the medium could not have recorded. What is left is inside the band, unmodelled, and does not reconstruct, so it has nowhere else to be.

==========================================================================
PROOF 30. Record level is not a new shape; its LEVEL DEPENDENCE is
    cited by: MATHEMATICS.md section 8b; models/magnetic.py
==========================================================================
  the control (linear depth, no cap): 1.01 of 5, expected 1.00
  the level axis WITH the cap:        2.90 of 5, condition 4.0
  the same WITHOUT the cap:           1.01 of 5
  with magnetic saturation:           2.29 of 5
  crossovers ['12.31', '6.15', '4.10', '3.08', '2.05'] MHz - the cap takes over lower as the level rises
  [PASS] the control reads one direction, as a linear depth law with no cap must
         got 1.01, expected 1.00
  [PASS] the level axis carries more than one direction
         got 2.90, expected > 2.0
  [PASS] and REMOVING THE CAP collapses it, so the cap is the entire source of the axis
         got 1.01, expected < 1.2
  [PASS] magnetic saturation REDUCES the axis, by compressing the depth range explored
         got 2.29 against 2.90, expected lower
  -- Record level's frequency shape is 0.9995 coherent with thickness loss and 1.0000 with spacing loss, so as a shape it is another member of the family that collapsed to 1.58 of 6. Its movement WITH level is a direction nothing else supplies, and that direction is the crossover between level-limited and cap-limited recording moving along the band. The control exists because a first estimate read 4.65 of 5 with the depth written as a fraction of the cap, which makes the response constant and measures numerical noise.

==========================================================================
PROOF 31. A new entry must be orthogonalised against the family it joins
    cited by: interference.signatures; magnetic.orthogonal_level_signature
==========================================================================
  the crossover at level 1.0 sits at 6.15 MHz
      carrier only: orthogonalised 6.88 of 14 at condition   19.9   |   raw 6.38 at   180.8
     VHS luma band: orthogonalised 8.45 of 14 at condition    7.4   |   raw 7.91 at     7.8
           full RF: orthogonalised 8.81 of 14 at condition   16.8   |   raw 8.39 at    17.3
  [PASS] orthogonalising improves the conditioning on every band
         got carrier only 19.9 against 180.8, VHS luma band 7.4 against 7.8, full RF 16.8 against 17.3, expected better everywhere
  [PASS] and the effective count with it
         got 8.45 against 7.91, expected higher
  [PASS] the orthogonalised entry is still subtractable
         got 0 failing, expected 0
  -- Three independent derivations reached this. Entered RAW most of the entry's shape duplicates the losses it sits beside, so it costs conditioning without adding a direction - on the carrier axis 6.37 effective at condition 159 against 7.00 at 11.7 without it. Projected orthogonal to them first it can be included on every band and improves them. This supersedes an earlier fix that simply excluded the entry where its crossover fell outside the band: that worked, but only by declining to use it.

==========================================================================
61 checks passed, 0 failed, 31 proofs run
==========================================================================
```
